runtime: python
env: flex
entrypoint: gunicorn -b :$PORT application:application

runtime_config:
  operating_system: "ubuntu22"
  python_version: "3.9"

# Resource settings
resources:
  cpu: 1
  memory_gb: 1
  disk_size_gb: 10

# Scaling settings
manual_scaling:
  instances: 1